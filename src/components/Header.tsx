import { Text } from '@chakra-ui/react';
import ContentBox from '@/components/ContentBox';

const Header = () => {
  return (
    <ContentBox>
      <Text>{'header'}</Text>
    </ContentBox>
  );
};

export default Header;
